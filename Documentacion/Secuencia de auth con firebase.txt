title Diagrama de autenticación con firebase
App->Firebase Auth:onAuthStateChanged() listener
alt Usuario autenticado
    Firebase Auth-->App:user object
    App->Firestore:getDoc(usuarios/user.uid)
    Firestore-->App:userData
    alt Si datosCompletos = true
        App->ViewRenderer:setCurrentView("menuPrincipal")
    else
        App->ViewRenderer:setCurrentView("formularioDatos")
    end
else Usuario no autenticado
    Firebase Auth-->App:null
    App->ViewRenderer:setCurrentView("menu")
end
note over App:Estado de loading = false
ViewRenderer->ComponenteCorrespondiente:Renderizar vista actual
